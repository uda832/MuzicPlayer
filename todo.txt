//Completed
    + Set up Song class
    + Query songs
    + Populate ListView
    + SongAdapter
    + Set up service (which will do the actual playing)
    + Notification && Jump to app from notif 
    + NOISY handler
    + Asset drop 
    + Bugfix: polish controller

//end-completed



Next Commit:
    + Place controller_bar view in main activity
    + InitControllerBar():
        + grabviews
        + create listeners
            + rootview -- Intent to go to NowPlayingFullscreen activity
            + play/pause
    + Asset drop: icons




Current Task:
    - Controller_bar: (Replace MediaController)

        - On activity resume:
            - if (isPlaying)
                call updateControllerBar()

        - UpdateControllerBar():
            - grab each view (album_art, title, artist, playbutton)
            - currentSong musicService.getSongPos()
            - controllerTitleText.setText(currentSong.getTitle)...

        - OnMetaDataChanged (will be called when a song changes etc.)
            - titleView.setText(new song's name etc)
        
    - Now Playing Activity
        - Action bar Setings menu
            - Add to Playlist
            - Settings
        - LongClick on Image Popup/Context menu
            - Go to Artist
            - Go to Album
        - Controls:
            - Soundcloud-like waveform to scroll through song
            - Fav button (auto add to "Fav" playlist)
            - Current time, total-current time 
            - Shuffle
            - Prev, Play/pause, Next

    



    

Bugs:
    - Ocassional inflate fail (reloaded on when scrolling viewpager)





Future Tasks:
    - Query and save albums artwork
        - Set songs artwork
    - Set up Fragments:
        - Songs
            - ListView item:
                - Artwork
                - Song name
                - Artist Name
            - LongClick for contextual action mode (CheckBox)
                - Menu:
                    - Add to playlist
                    - Settings
        - Albums
            - GridView
                - Artwork
                - Album name
                - Artist
            - Single Album:
                - 1/3 Screen with art, name, artist, year, total duration
                - ListView Tracks (trackNum, TrackName, duration)
        - Artists:
            - ListView Item:
                - first album artwork, artistName, numTracks 
            - Single Artist:
                - Title=ArtistName
                - Expandable ListView Item:
                    - Album(art, year)
                        - ontouch to expand/collapse
                        - tracks ListView
        - Playlists
    - SongAdapter:
        - Need to grab images (leave artwork for now)
    * Contextual Action Menu
        * Add to playlist
    * Alphabetical Scroll Bar



Possible Features:
    * Full Access widget on Lock Screen
    * Background: fill/fade with current artwork 
    * Background: soundcloud-like animate according to playtime

